<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicalui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedicalUi">
        <activity android:name=".SendedCallSplashActivity"></activity>
        <activity android:name=".SelectDoctorActivity" />
        <activity android:name=".CreateCallActivity" />
        <activity android:name=".CallsActivity" />
        <activity android:name=".ReceptionistActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SecondSplashActivity" />
        <activity android:name=".FirstSplashActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>